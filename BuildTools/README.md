``` console
$ git clone https://github.com/uber/mockolo.git mockolo-workspace
$ cd mockolo-workspace
$ ./install-script.sh -s Sources -t mockolo -d out -o bin.tar.gz
$ cd ..
$ tar -xvf mockolo-workspace/bin.tar.gz
```
